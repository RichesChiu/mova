services:
  app:
    image: richeschiu/mova:latest
    # External PostgreSQL: replace MOVA_DATABASE_URL below, remove this
    # depends_on block, and remove the database service at the end of this file.
    depends_on:
      database:
        condition: service_healthy
    ports:
      - "36080:36080"
    environment:
      # Internal Compose database connection. Keep this password in sync with database.POSTGRES_PASSWORD.
      MOVA_DATABASE_URL: "postgres://mova:postgres@database:5432/mova"
      # Optional: TMDB API Read Access Token. Leave empty to skip remote metadata scraping.
      MOVA_TMDB_ACCESS_TOKEN: ""
      # Optional: proxy address reachable from the container, for example http://192.168.1.10:7890.
      HTTP_PROXY: ""
      HTTPS_PROXY: ""
    volumes:
      - ./data/cache:/app/data/cache
      - type: bind
        # Replace with the absolute media directory on the Docker host.
        source: /absolute/path/to/media
        target: /media
        read_only: true
    restart: unless-stopped

  database:
    # Delete this entire service when MOVA_DATABASE_URL points to external PostgreSQL.
    image: postgres:18
    environment:
      POSTGRES_USER: mova
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: mova
    volumes:
      - ./data/postgres:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mova -d mova"]
      interval: 5s
      timeout: 5s
      retries: 12
    shm_size: 256mb
    restart: unless-stopped
