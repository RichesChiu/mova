services:
  rust-tooling:
    image: richeschiu/mova-rust-build-base:1-bookworm
    working_dir: /app
    environment:
      HTTP_PROXY: ${HTTP_PROXY:-}
      HTTPS_PROXY: ${HTTPS_PROXY:-}
      NO_PROXY: ${NO_PROXY:-localhost,127.0.0.1,::1,database}
      ALL_PROXY: ${ALL_PROXY:-}
      http_proxy: ${HTTP_PROXY:-}
      https_proxy: ${HTTPS_PROXY:-}
      no_proxy: ${NO_PROXY:-localhost,127.0.0.1,::1,database}
      all_proxy: ${ALL_PROXY:-}
      CARGO_HOME: /usr/local/cargo
    volumes:
      - .:/app
      - mova-cargo-registry:/usr/local/cargo/registry
      - mova-cargo-git:/usr/local/cargo/git
      - mova-rust-target:/app/target
    profiles: ["tooling"]
    command: ["cargo", "--version"]

  app:
    image: mova:local
    build:
      context: ${MOVA_BUILD_CONTEXT:-.}
      dockerfile: apps/mova-server/Dockerfile
      args:
        MOVA_BUILD_VERSION: ${MOVA_BUILD_VERSION:-development}
        HTTP_PROXY: ${HTTP_PROXY:-}
        HTTPS_PROXY: ${HTTPS_PROXY:-}
        NO_PROXY: ${NO_PROXY:-localhost,127.0.0.1,::1,database}
        ALL_PROXY: ${ALL_PROXY:-}

volumes:
  mova-cargo-registry:
  mova-cargo-git:
  mova-rust-target:
