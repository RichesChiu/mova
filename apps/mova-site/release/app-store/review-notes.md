# App Review preparation

## Required before submission

MOVA requires a server and account, so App Review must receive a stable public HTTPS demo environment. Do not submit until all fields below are real and tested from a network outside the developer's home or office.

- Demo server URL: `[REQUIRED]`
- Administrator username: `[REQUIRED]`
- Administrator password: `[REQUIRED]`
- Demo server expiry: must remain available throughout review
- Demo content: use media that the developer owns, created, or may legally distribute for review

Never use a local-network address, expiring one-time password, or production account containing private media.

## Suggested App Review Notes (English)

MOVA is a native client for a user-selected, self-hosted MOVA media server. The app includes no media content and does not provide a public streaming catalog.

To review the app:

1. Launch MOVA and choose Add Server.
2. Enter the demo server URL and the administrator credentials supplied in the Sign-in Required fields.
3. Open Home to review libraries, recently added titles, and continue watching.
4. Open a series to review seasons, episodes, cast, and media resource details.
5. Play a demo item to test seeking, audio tracks, subtitles, and playback progress.
6. Open Server Settings to review users, libraries, scan progress, and notifications.

The demo server is reachable over public HTTPS and will remain online during review. The supplied account is an administrator so all submitted functionality can be tested.

Privacy notes:

- The app has no advertising, tracking, or developer-operated analytics.
- Credentials are sent directly to the selected MOVA server.
- Access and refresh tokens are stored in the macOS Keychain.
- Server configuration and preferences are stored locally on the Mac.

Local Network permission is used only when a user chooses a MOVA server on the same local network. The review demo server does not require local-network access.

## Review contact

- Display name: `Riches Chiu`
- Legal individual: `赵晨光 / Chenguang Zhao`
- Email: `riches.chiu@gmail.com`
- Phone: `[REQUIRED IN APP STORE CONNECT]`

## Encryption questionnaire

The app uses standard operating-system networking to connect over HTTPS and stores tokens in the macOS Keychain. Answer the App Store Connect export-compliance questionnaire against the final binary. Do not claim an exemption solely from this document; retain the answers shown by App Store Connect with the release record.
