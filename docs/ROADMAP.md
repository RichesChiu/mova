# Mova 功能现状与开发路线

这份文档只描述当前代码里已经落地的能力，以及接下来继续实现时最值得优先投入的部分。

如果你关心当前工程结构是否合理、哪些模块应该拆、目录后续该如何收敛，见 [`docs/ARCHITECTURE.md`](/Users/riches/Desktop/mova/docs/ARCHITECTURE.md)。

## 1. 当前已实现的能力

### 基础服务

- `mova-server` 可启动 Axum HTTP 服务。
- 启动时会自动连接 PostgreSQL、执行 migration、再做一次 `ping`。
- `GET /api/health` 会同时检查服务进程和数据库可用性。
- `mova-web` 已提供基于 React + Vite 的前端原型，可直接联调现有 API。

### 用户与认证

- 支持查询 bootstrap 状态 `GET /api/auth/bootstrap-status`。
- 当系统里还没有管理员时，支持初始化首个管理员 `POST /api/auth/bootstrap-admin`。
- 支持登录、登出、当前用户查询和当前用户昵称更新 `POST /api/auth/login` / `POST /api/auth/logout` / `GET /api/auth/me` / `PATCH /api/auth/me`。
- 当前用户角色区分为 `admin` / `viewer`。
- `admin` 默认拥有全部媒体库访问权；`viewer` 只可见被分配的媒体库。
- 已支持管理员查询和创建用户 `GET /api/users` / `POST /api/users`。
- 已支持管理员更新用户基础信息、删除用户和重置密码 `PATCH /api/users/{id}` / `DELETE /api/users/{id}` / `PUT /api/users/{id}/password`。
- 已支持为普通用户更新媒体库访问范围 `PUT /api/users/{id}/library-access`。
- 已支持当前登录用户修改自己的密码 `PUT /api/auth/password`，修改后会轮换 session。
- 除健康检查和 bootstrap / 登录相关接口外，现有浏览、播放和管理接口都已经挂到 session 鉴权下。

### 媒体库管理

- 支持创建媒体库 `POST /api/libraries`。
- 支持查询媒体库列表 `GET /api/libraries`。
- 支持查询媒体库详情 `GET /api/libraries/{id}`。
- 支持更新媒体库基础配置 `PATCH /api/libraries/{id}`。
- 支持删除媒体库 `DELETE /api/libraries/{id}`。
- 媒体库类型当前支持 `mixed` / `movie` / `series`，其中 `mixed` 会按单文件自动判断电影或剧集。
- 创建媒体库时会校验 `root_path` 是否存在且为目录。
- 新建媒体库后会立即出现在前端，但不会自动开始扫描；当前只保留手动 `Scan Library`。
- 删除媒体库时会先阻止新的扫描进入，并等待当前扫描安全停止后再执行删除。

### 扫描任务

- 支持为媒体库创建异步扫描任务 `POST /api/libraries/{id}/scan`。
- 支持查询扫描历史 `GET /api/libraries/{id}/scan-jobs`。
- 支持查询单个扫描任务状态 `GET /api/libraries/{id}/scan-jobs/{scan_job_id}`。
- 扫描任务状态目前包含 `pending / running / success / failed`。
- 扫描发现文件时会实时更新 `scan_jobs.scanned_files`。
- 支持 `GET /api/events` 的 SSE 实时事件流，当前会主动广播 `scan.job.updated`、`scan.job.finished`、`scan.item.updated`、`library.updated`、`library.deleted` 和 `media_item.metadata.updated`。
- 扫描实时事件已补上 `discovering / enriching / syncing / finished` 阶段，并会在发现新电影文件或新剧集目录组时立刻推送条目级占位信息；同一张卡会继续经历元数据、图片和等待写库阶段。
- 扫描失败时，`scan_job.error_message` 现在会直接带阶段上下文，例如扫描目录失败、元数据补全失败、写库失败。
- TMDB / OMDb 请求现在有明确超时，远端 metadata 失败时会更快回退到本地解析，不会把整轮扫描长时间卡在 enrichment 阶段。
- 对于 `file_path` 未变化、且过去已经成功补过 overview / 海报 / 标题等 metadata 的条目，重扫时会先把数据库里的已有 metadata 回填到扫描结果，再决定是否继续走远端 enrichment，避免无意义地重复请求 TMDB / OMDb 和重新下载图片。
- 扫描写库会先尝试整库事务同步；如果因为单条脏数据导致整批写入失败，会自动回退到逐条 best-effort 写入，尽量保住其余正常条目继续入库。

### 扫描入库

- 会递归扫描目录下的常见视频文件扩展名。
- 扫描结果会写入 `media_items` 和 `media_files`。
- 同一部电影如果本地存在多个不同封装或不同版本文件，当前会复用同一个 `movie media_item`，再把多个 `media_files` 挂到这条电影下面。
- 剧集文件当前会聚合写入 `series`、`seasons`、`episode media_items` 和 `media_files`。
- 当前扫描会按 `(library_id, file_path)` 做增量同步。
- `mixed` 库会按单个文件判断类型：命中 `S01E02`、`1x02`、`Season 01/` 等剧集信号时按 `episode` 处理，否则默认按 `movie` 处理。
- 本地剧集识别已补到不依赖 TMDB 的常见目录命名：`Season 01/01.mkv`、`EP02`、`Episode 03`、`第03集` 这类文件会直接推导出剧名、季号和集号，避免远端超时后整部剧被拆散。
- 同一季同一集现在支持保留多个本地文件版本：会复用同一个 `episode media_item`，再把不同文件挂到同一集下面，不会因为第二个版本写入而整轮扫描失败。
- 同路径文件会原地更新，保持原有 `media_item_id` / `media_file_id`。
- 改名或移动路径的文件会表现成旧路径删除、新路径新增。
- 当本地 `.nfo`、`poster`、`fanart` 等 sidecar 文件变化时，系统会刷新同目录受影响的视频文件。
- 当前不再常驻文件 watcher，也不再默认做后台自动扫库；新增、删除、改名和移动统一通过手动扫库收敛，减轻网络盘和 Docker 挂载场景下的运行时负载。
- 可以通过 `GET /api/libraries/{id}/media-items` 查询某个库下的已入库媒体条目。

### 媒体详情

- 支持查询单个媒体条目 `GET /api/media-items/{id}`。
- 支持查询某个媒体条目关联的文件列表 `GET /api/media-items/{id}/files`。
- 电影详情页现在会先给播放版本选择；关联媒体文件也会收成同一条目下的 source details 与并排的视频、音频、字幕三类子卡。版本本身在播放区切换，音轨和字幕再各自通过卡头小下拉切换，并直接展示体积、路径、分辨率、码率、色彩信息、音轨参数，以及字幕的默认、强制、听障和外挂标记，方便比较同名多版本资源。
- 电影详情页标题旁现在可直接展示带 `IMDb` 标识的评分；配置可选的 `MOVA_OMDB_API_KEY` 后，扫描与单条元数据刷新会补齐 `imdb_rating`。
- 电影详情页的 hero 现在会优先用背景图或海报做模糊大底，并叠加渐变和光晕层，让海报、标题和 IMDb 评分有更完整的观影页质感；顶部重复的 muted 副标题已经收掉，年份、国家/地区、题材类型和工作室统一收进 facts 区。
- 媒体详情页的演员列表已经拆成独立请求；详情主体会先渲染，再异步补演员，且演员缓存改成在扫库与手动 metadata 刷新阶段预取，不再由详情页请求触发远端 cast 拉取。
- 本地海报/背景图对外返回的资源 URL 现在会带版本参数，并配合浏览器缓存头，减少库页和详情页里封面周期性重拉导致的短暂闪烁。
- 支持查询某个剧集条目的季列表 `GET /api/media-items/{id}/seasons`。
- 支持查询某一季下的集列表 `GET /api/seasons/{id}/episodes`。
- 支持查询某一季的海报和背景图 `GET /api/seasons/{id}/poster` / `GET /api/seasons/{id}/backdrop`。
- 支持查询剧集“全集大纲 + 本地可用集” `GET /api/media-items/{id}/episode-outline`（TMDB 可用时返回全集，缺失集可前端置灰）。
- TMDB 剧集大纲会缓存到 PostgreSQL（24h TTL），减少重复请求；缓存过期且 TMDB 失败时回退旧缓存。
- TMDB 可提供季海报和集剧照；剧照会用于集级封面/背景。TMDB 缺图时，会尝试用本地视频首帧回退（依赖 `ffmpeg`）。
- 支持管理员手动搜索单条媒体的候选元数据并应用匹配结果 `GET /api/media-items/{id}/metadata-search` / `POST /api/media-items/{id}/metadata-match`。
- 手动匹配后会持久化 TMDB 条目 ID，后续演员缓存和剧集大纲会优先走精确绑定。
- 支持手动重拉单个媒体条目元数据 `POST /api/media-items/{id}/refresh-metadata`。
- 支持查询某个媒体条目的海报和背景图 `GET /api/media-items/{id}/poster` / `GET /api/media-items/{id}/backdrop`。
- `media_files` 表里的基础字段已经可以通过 HTTP API 读出来。

### 播放链路

- 支持 `GET /api/media-files/{id}/stream` 播放媒体文件。
- 支持 `HEAD /api/media-files/{id}/stream` 读取播放相关响应头。
- 支持 `GET /api/media-files/{id}/audio-tracks` 查询媒体文件可切换的内嵌音轨列表。
- 支持基础单段 `Range` 请求，可满足浏览器视频拖动和分段读取。
- 支持 `GET /api/media-files/{id}/subtitles` 查询媒体文件可切换字幕列表。
- 支持 `GET /api/subtitle-files/{id}/stream` 输出 WebVTT 字幕轨道。
- 前端播放器已支持源切换、字幕切换、音轨切换、缓冲状态提示、当前源重试和“从头播放”入口。
- 播放器页现在会优先等待可播放文件列表返回；播放进度查询即使较慢或失败，也不会再把整页长期卡在 `Loading player…`。
- 切换到非默认音轨时，服务端会用 `ffmpeg -c copy` 生成缓存的 remux 变体，再继续走同一条媒体流接口，不需要整段转码。
- 音轨菜单现在会显示当前选中状态，并在切换中、切换完成和音轨列表加载失败时给出更明确的用户提示，而不是直接暴露底层接口错误。
- 播放器现在会区分媒体源失败、自动播放被浏览器拦截、字幕加载失败和播放进度保存失败；字幕失败和进度保存失败都按非阻断 warning 处理，不会直接打断主视频播放。
- 全屏不可用或被浏览器/宿主窗口拦截时，播放器也会给出非阻断提示，不再和媒体源播放失败混成同一类错误。

### 播放进度

- 支持 `GET /api/media-items/{id}/playback-progress` 查询单个媒体条目的最近播放进度。
- 支持 `PUT /api/media-items/{id}/playback-progress` 更新播放位置和完成状态。
- 支持 `GET /api/playback-progress/continue-watching` 获取未看完内容的继续观看列表。
- 支持 `GET /api/watch-history` 获取当前用户自己的观看历史会话列表。
- 播放进度已经切到真实登录用户，不再挂在内部默认本地用户上。
- 播放进度、继续观看和未来用户态数据都按 `user_id` 维度隔离，不同用户互不共享。
- `playback_progress` 负责“当前最新状态”，`watch_history` 负责“一次次观看会话”的历史记录。
- 前端播放页已切到轮询上报 + 离场强制 flush，并在切源/切集前补报当前位置。
- 播放器现在会在接近片尾时把这次观看判定为“已看完”，减少浏览器没有可靠触发 `ended` 时继续观看列表残留的问题。

### 最小元数据解析

- 扫描时会基于文件名提取清洗后的标题。
- 当文件名里混入发布组、编码参数或 HTML 实体时，扫描会优先参考更干净的父目录名称来回推电影标题，但像“合集 / collection / box set”这类目录不会再覆盖掉下面每一部电影自己的标题。
- 常见电影文件名里的四位年份会写入 `media_items.year`。
- 扫描时会读取本地 sidecar 元数据，包括 `movie.nfo` / `<video>.nfo`、`poster.jpg`、`fanart.jpg` 等常见文件。
- 设置 `MOVA_TMDB_ACCESS_TOKEN` 后，扫描会额外查询 TMDB，补齐缺失的 `overview`、`poster_path`、`backdrop_path`、`original_title`、`year`；本地文件名或目录里解析出的年份会先参与搜索，但如果这个年份写错了，系统会自动回退到不带年份的搜索结果。
- 如果额外配置 `MOVA_OMDB_API_KEY`，会在 TMDB 已拿到 `imdb_id` 的前提下补齐 `imdb_rating`，供详情页标题旁展示。
- TMDB 配置当前从运行时环境变量读取，媒体库仍可单独配置元数据语言。
- 扫描命中 TMDB 图片时，会优先把海报和背景图下载到本地缓存目录，再写入媒体记录。
- `overview`、`original_title`、`sort_title`、`poster_path`、`backdrop_path` 已可通过现有详情接口返回。
- 当环境里有 `ffprobe` 时，扫描还会填充 `media_files` 的时长、编码、分辨率和码率。

### 管理端与前端测试

- 设置页已支持用户创建、编辑、删除、启停和媒体库访问授权。
- 设置页已支持媒体库扫描、删除，以及名称、描述和元数据语言编辑，并直接展示最近一次扫描状态摘要；建库入口也已收进弹窗，不再把整块表单铺在页面底部。
- 个人页已收成单块资料面板，展示用户名、昵称、角色标签和链接式改密入口；昵称支持行内编辑并会同步反映到顶栏用户区。
- 页面里的轻量帮助提示现在走根级浮层，不再因为卡片或容器的 `overflow` 被裁掉。
- 当前前端产品文案已先统一为英文，为后续补中英切换做准备。
- 首页库卡、首页库 shelf 和媒体库页已支持扫描中库进度条，以及条目级“发现文件 -> 补元数据 -> 获取海报简介 -> 等待写库”的连续占位卡提示；即使还没发现第一条媒体，也会先显示扫描中的 loading 状态。
- 首页库卡的标题和扫描文案现在会尽量维持单行或固定层级，避免长资源名和长状态文案在同步过程中把卡片高度来回撑动。
- 首页库卡和媒体库页现在也会直接提示最近一次扫描失败，用户不需要进入管理页才能知道这轮同步出了什么问题。
- 扫描中的媒体占位卡现在会尽量保持和最终卡片接近的尺寸，减少写入媒体库完成前后的列表抖动。
- 媒体详情页现在也会感知所在媒体库的实时扫描状态：hero 区会提示当前条目或当前媒体库仍在同步；剧集详情页会把当前季相关的扫描中集卡并入 episode rail，而不是等写库后再突然出现。
- 单库详情页的头部信息也进一步收成内容页样式：优先展示库标题和描述，类型与数量只作为轻量统计，不再重复“Library Name / Description”这类配置式文案。
- 单库详情页和媒体详情页的返回入口现在统一成轻量文本链接，不再沿用按钮式工具条，减少头部区域的厚重感。
- 新建启用媒体库后，同一端会先显示一个扫描中的新库占位，再由真实接口结果接管。
- 设置页里的删库和删用户现在走统一确认弹窗，并会在弹窗内直接回显删除失败原因，而不是依赖原生 `window.confirm`。
- 顶栏用户区左侧已预留中英语言下拉 UI，当前只做本地交互占位，后续可直接接入真正的中英文案切换；用户显示名现在优先走昵称，没有昵称时回退到用户名。
- 媒体卡标题现在会固定单行省略，避免扫描中或长文件名场景把首页和媒体库页的卡片高度撑来撑去。
- 前端已接入 `Vitest + React Testing Library + jsdom` 的最小测试底座。
- 当前前端测试策略更偏向“高风险 hook + 播放器交互 + 纯函数 helper”：已覆盖实时事件 hook 的断线恢复、媒体库实时刷新、元数据实时刷新和删库跳转；扫描运行时文案与进度计算；播放器恢复进度、`from-start`、切源迁移，以及自动播放/全屏/字幕失败的非阻断降级；以及播放入口、媒体库编辑配置、设置页缓存更新/删除边界这类纯函数决策。
- 后端已补上播放进度、SSE、用户 CRUD 和媒体库 CRUD 的基础测试：默认 `cargo test` 会跑纯单测；依赖 `DATABASE_URL` 的播放进度 / SSE / 用户管理 / 媒体库运行时数据库集成测试已写好，当前也覆盖了 SSE 库级可见性过滤和扫描接口的 `admin-only` 权限边界，但默认标记为 `ignored`，需要在可用的 PostgreSQL 测试库上显式执行。

## 2. 当前实现边界

这些不是 bug，而是当前系统还没做完的边界：

- 剧集聚合当前依赖文件名或目录结构里出现明确的季集信号；如果用户把电影误放进 `series` 库，或者剧集文件名里没有可解析的季集号，系统可能跳过这些文件。
- 没有 `ffprobe` 或探测失败时，时长、编码、分辨率、码率等字段仍会为空。
- 当前元数据增强已支持本地 sidecar、TMDB 自动补全、TMDB 图片本地缓存、单条媒体项手动重拉，以及管理员手动重匹配；但按当前产品方向，MVP 不会把批量人工修正、字段锁定和复杂治理流程当作必需能力，缺失元数据允许保持为空。
- `watch_history` 当前只记录用户自己的观看会话；还没有单独的“已看过 / 看过几次 / 最近看完”聚合接口。
- 扫描时不会解析剧集号、季号、演员、类型等更完整的结构化元数据。
- `scan_jobs.scanned_files` 目前仍表示“已发现文件数”；后续阶段进度主要依赖 SSE 里的 `phase` 和条目级实时事件，而不是单独落库字段。
- 条目级扫描卡当前以 `item_key` 做临时身份标识：电影通常是 `file_path`，剧集会优先使用系列目录路径；在最终写库完成前它仍然不是数据库里的真实媒体项 ID。
- 当前只对“同路径未变化”的文件保持稳定 ID；改名或移动后的路径会被当成新文件处理。
- 现在不再依赖 watcher 自动同步；网络盘、Docker 挂载和本地目录都统一走“手动扫库”这条更可控的路径。
- 扫描任务是通过 API 进程内的 `tokio::spawn` 执行的，服务重启后任务不会恢复。当前做法是在启动时把中断任务标记为失败，允许手动重新触发。
- 当前已经有基础直链播放、字幕和内嵌音轨切换，也补上了错误分类、重试、自动播放/全屏兼容 warning 和非阻断降级；但还没有转码、自适应码率和更细的播放器兼容性治理。
- 当前用户体系已经有最小闭环，并补上了用户编辑、用户删除和密码修改；但还没有会话管理和更完整的用户管理 UI。

## 3. 功能盘点

| 模块 | 状态 | 当前情况 | 主要缺口 |
| --- | --- | --- | --- |
| 服务启动与配置 | 已实现 | 支持环境变量、构建时内置默认值、自动 migration、数据库连通性检查 | 缺少更细的部署说明和发布流程约束 |
| 健康检查 | 已实现 | `GET /api/health` 可用 | 缺少更细粒度的 readiness / liveness 区分 |
| 媒体库管理 | 部分实现 | 支持创建、列表、详情、删除，以及名称、描述和元数据语言更新；删除时会安全停止该库当前扫描；前端设置页可直接编辑这些基础配置并查看最近扫描状态，删库/删用户也已有统一确认弹窗，建库时也补了库类型差异和容器路径映射提示 | 还缺更完整的库级管理体验打磨，以及更多前端回归测试 |
| 扫描任务 | 部分实现 | 支持异步触发、历史查询、单任务轮询、同库活跃任务复用、发现阶段实时进度、SSE 阶段广播，以及首页/库页/详情页里“发现即渲染”的条目级扫描卡和失败提示 | 缺少更完整的全局扫描态展示、更多页面对条目级扫描态的复用，以及状态文案打磨 |
| 文件系统扫描 | 部分实现 | 能递归发现常见视频文件，按 `(library_id, file_path)` 做增量同步，并通过手动扫库收敛新增、删除、改名和移动 | 缺少忽略规则、软链策略、去重、可选的低频定时扫库策略 |
| 媒体建模 | 部分实现 | 已支持电影直写，以及剧集聚合成 `series / seasons / episode media_items / media_files`；`mixed` 库可按文件初步区分电影和剧集 | 缺少多版本文件建模、更稳的剧集识别和更丰富的剧集元数据 |
| 元数据解析 | 部分实现 | 已支持基础标题清洗、年份提取、本地 sidecar 元数据、TMDB 缺失字段补全、TMDB 图片本地缓存、单条媒体项元数据重拉、管理员手动重匹配，以及基于 `ffprobe` 的最小时长/编码/分辨率探测 | 当前产品方向不把人工元数据治理纳入 MVP；主要缺口是继续提升自动补全成功率和缺失时的展示兜底 |
| 媒体浏览 | 部分实现 | 可按媒体库分页列出媒体条目，支持名称筛选、发行年筛选，并查询单条详情、文件列表和本地图片资源；详情页已可横向比较同一条目下不同资源文件的视频、音频、字幕技术信息，并通过小下拉切换不同轨道/字幕；前端原型已能联调媒体库、详情和剧集聚合 | 缺少更多排序方式、更多筛选条件、播放页与扫描联动 |
| 播放链路 | 部分实现 | 支持媒体文件直链播放、基础单段 `Range`、字幕查询与挂载、内嵌音轨查询与切换、单条播放进度读写、继续观看列表和用户观看历史；前端播放器已有缓冲提示、重试当前源、从头播放、切换补报、错误分类、接近片尾自动完成，以及字幕/进度保存、自动播放、全屏失败的非阻断降级 | 缺少转码、自适应码率和更完整的播放器兼容性验证 |
| 用户与认证 | 部分实现 | 已支持 bootstrap 首个管理员、登录/登出、当前用户、`admin / viewer` 角色、按库授权、管理员创建/编辑/删除用户，以及管理员重置密码和用户自改密码 | 缺少会话治理和更完整的前端用户管理 |
| 运维与后台任务 | 部分实现 | 启动日志、扫描失败日志已有 | 缺少结构化监控、统一任务中心和更多运行态可见性 |
| 工程结构 | 部分实现 | 当前 crate 分层基本清晰，`data/` 运行时目录也已统一；前端、服务端和各 Rust crate 都已有各自 README 说明入口与职责 | 大文件职责过重，server 侧模块仍偏平铺，缺少进一步的 feature-first 收敛 |
| 测试 | 部分实现 | 后端已有实时事件和扫描相关单测，并补上了播放进度 / SSE / 用户 CRUD / 媒体库 CRUD 的数据库集成测试样例，当前也覆盖了 SSE 库级可见性过滤与扫描接口权限边界；前端已接入 Vitest，并覆盖实时事件 hook、扫描运行时纯函数、设置页缓存 helper、播放入口 helper 和播放器关键恢复路径 | 仍缺更多扫描链路测试、更多 SSE/HTTP 权限边界补充、设置页 CRUD 回归测试，以及跨浏览器的播放器手工验证 |
## 4. 距离 MVP 还差什么

如果把 MVP 定义成“管理员只负责用户和媒体库的增删改查；系统自己处理扫描和同步；所有可见用户都能感知扫描状态；普通用户可浏览、可播放、可续播；系统可在单机部署下稳定自用”，当前离 MVP 主要还差下面几块：

1. 扫描态继续前台化

- 当前已经有扫描阶段、首页库进度条和条目级占位卡，但还要继续保证“新建库后所有有权限的用户都能稳定看到扫描中”和更多页面的一致反馈。
- 重点不是任务列表，而是让系统把扫描过程自然地展示在用户已经会看的界面里。

2. 管理端进一步收口

- 设置页已经能做用户管理、媒体库扫描、媒体库删除，以及媒体库名称、描述和元数据语言编辑；主要管理弹窗也已经统一成更轻量的关闭样式和更稳定的表单布局。
- 按当前方向，管理员不需要关注任务内部细节，也不需要承担元数据治理；下一步更偏向把现有用户与媒体库 CRUD 继续打磨顺滑，而不是继续扩更多后台面板。

3. 播放链路最后一段稳定化

- 当前已经有字幕、音轨切换、切源、重试、缓冲提示、继续播放和从头播放，也补上了更清晰的错误分类与基础播放器回归测试；但还缺更多边界验证，例如不同浏览器兼容性和更多实际设备上的恢复路径。
- 这部分决定“能播”是否能升级成“稳定可播”。

4. 测试与文档收口

- 现在前端已有最小 Vitest，测试重点已收口到高风险 hook、播放器和纯函数 helper；后端也补上了部分数据库集成测试样例，并开始覆盖扫描与 SSE 的权限边界，但还缺更多扫描链路、HTTP 权限边界和设置页 CRUD 回归测试。
- MVP 形态的部署/升级说明已经单独落到 [`docs/DEPLOYMENT.md`](/Users/riches/Desktop/mova/docs/DEPLOYMENT.md)，后续主要是随着功能继续同步更新，而不是从零开始补文档。

## 5. 下一步最值得做的功能

按当前代码形态，建议优先级如下：

1. 扫描态在更多浏览入口的一致展示

- 继续把首页、媒体库页以外的浏览入口也接上更自然的扫描中提示。
- 目标是用户只看日常页面，也能感知库和条目仍在同步，不需要理解后台任务细节。

2. 剩余播放器边界与兼容性验证

- 重点放在真实浏览器里的自动播放、全屏、字幕格式兼容和异常源恢复，而不是继续堆新功能。
- 这部分更偏“把现在已经有的能力做稳”。

3. 设置页 CRUD 的前端回归测试

- 前端这边还需要把设置页里最常用的创建、编辑、删除和授权路径再锁一层。
- 这样后续继续打磨管理端时，不容易把当前已经可用的最小闭环弄坏。

4. 扫描与权限边界的后端集成测试

- 继续补扫描任务 HTTP、SSE 权限过滤和更完整的数据库集成测试。
- 当前已补上 SSE 库级可见性过滤，以及扫描历史 / 单任务详情 / 手动触发扫描的 `admin-only` 边界。
- 这样后面继续改扫描态前台化和管理端时，后端行为更不容易被静默改坏。

## 6. MVP 前最该补的测试

- 扫描任务的 HTTP 集成测试：创建、复用活跃任务、查询历史、查询单任务状态，以及阶段状态/进度字段。
- SSE 权限边界测试：确认 `scan.job.*`、`library.*`、`media_item.metadata.updated` 只推给有权限的用户；当前已补上 `library.updated` 和 `scan.item.updated` 的过滤样例。
- 条目级扫描进度测试：元数据补全和图片获取阶段的事件是否按预期推送。
- 设置页 CRUD 回归测试：创建库、编辑库、删除库，以及用户授权。
- 跨浏览器的播放器手工验证：至少覆盖自动播放限制、全屏、字幕格式和错误源重试。
